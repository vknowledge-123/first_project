use sql830;
CREATE TABLE `Pos_Salaries` (
	`Position` VARCHAR(17) NOT NULL, 
	`Level` DECIMAL(38, 0) NOT NULL, 
	`Salary` DECIMAL(38, 0) NOT NULL
);
select * from `Pos_Salaries`;
select * from hotel_data;
select * from small_data;
